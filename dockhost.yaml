version: 1
project: vscode-tunnel

repositories:

    vscode-tunnel:

        url: https://github.com/lynx-r/vscode-tunnel-test.git
        branch: main

        # Set CPU and Memory for first container startup

        cpu: 1
        cpuFraction: 20
        memory: 128MiB
